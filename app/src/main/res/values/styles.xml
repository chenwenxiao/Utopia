<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="numberPickerStyle">@style/customNumberPicker</item>
        <item name="android:actionBarStyle">@style/ActionBar</item>

    </style>

    <style name="customNumberPicker" parent="@style/NPWidget.Holo.Light.NumberPicker">
        <item name="selectionDividerHeight">1dp</item>
        <item name="internalMaxHeight">120dp</item>
        <item name="internalMinHeight">30dp</item>
        <item name="internalMinWidth">30dp</item>
        <item name="internalMaxWidth">40dp</item>
        <item name="selectionDividersDistance">40dp</item>
        <item name="selectionDivider">@drawable/numberpicker_selection_divider</item>
    </style>

    <style name="ActionBar" parent="@android:style/Widget.Holo.ActionBar">
        <item name="android:background">@color/darkPink</item>
    </style>

    <style name="Dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.2</item>
    </style>

</resources>
